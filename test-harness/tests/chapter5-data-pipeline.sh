#!/bin/bash
# Smoke test for Chapter 5 Data Pipeline
#
# NOTE: This chapter is intentionally not tested. It's a simpler version of
# chapter6 which has the same pipeline architecture but includes unit and
# integration tests. Chapter 6's tests provide coverage for this pattern.
#
# The pipeline architecture (S3 -> Lambda -> SNS -> Lambda) is tested via
# chapter6's Maven integration tests instead.

exit 0
